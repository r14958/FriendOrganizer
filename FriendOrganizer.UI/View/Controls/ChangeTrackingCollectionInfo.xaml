<UserControl x:Class="FriendOrganizer.UI.View.Controls.ChangeTrackingCollectionInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FriendOrganizer.UI.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" Background="{StaticResource IsChangedBackgroundBrush}"
                Visibility="{Binding IsChanged, Converter={StaticResource BooleanToVisibilityConverter}}"
                TextElement.Foreground="{StaticResource MahApps.Brushes.ThemeForeground}">
        <TextBlock Text="Added:  " Margin="10 0 0 0"/>
        <TextBlock Text="{Binding AddedItems.Count}" MinWidth="35"/>
        <TextBlock Text="Removed: " Margin="10 0 0 0"/>
        <TextBlock Text="{Binding RemovedItems.Count}" MinWidth="35"/>
        <TextBlock Text="Modified: " Margin="10 0 0 0"/>
        <TextBlock Text="{Binding ModifiedItems.Count}" MinWidth="35"/>
    </StackPanel>
</UserControl>
